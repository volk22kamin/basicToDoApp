pipeline {
    agent {
        kubernetes {
            label 'docker-agent'
        }
    }

    stages {
        stage('build image') {
            steps {
                script {
                    container('docker') {
                        sh 'docker build -t basic-to-do-client:1.1.7 .'
                    }
                }
            }
        }
    }
}
