pipeline {
    agent {
        kubernetes {
            label 'k8s-agent'
        }
    }

    stages {
        stage('build image') {
            steps {
                script {
                    sh "docker build"
                }
            }
        }
    }
}
