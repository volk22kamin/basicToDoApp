apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-to-do-app.fullname" . }}
  labels:
    {{- include "basic-to-do-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-to-do-app.selectorLabels" . | nindent 4 }}
